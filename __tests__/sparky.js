/* eslint-env jest */

describe.skip('Streaming incoming files', () => {
  it('handles new files', () => {

  })

  it('handles edited files', () => {

  })

  it('handles removed files', () => {

  })
})

describe.skip('Streaming local file changes', () => {
  describe('File add', () => {
    it('triggers a stream', () => {

    })
    it('streams the file contents', () => {

    })
  })

  describe('File change', () => {
    it('triggers a stream', () => {

    })
    it('streams the file contents', () => {

    })
  })

  describe('File remove', () => {
    it('triggers a stream', () => {

    })
    it('has no file contents to stream', () => {

    })
  })
})
